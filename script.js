const intro=document.querySelector('#brandIntro'),header=document.querySelector('#siteHeader'),progress=document.querySelector('#scrollProgress'),menuBtn=document.querySelector('#menuBtn'),mobileMenu=document.querySelector('#mobileMenu'),watchFilm=document.querySelector('#watchFilm'),filmModal=document.querySelector('#filmModal'),closeFilm=document.querySelector('#closeFilm'),modalVideo=document.querySelector('#modalVideo');
const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches;
function finishIntro(){intro?.classList.add('hide');document.body.classList.remove('intro-lock');setTimeout(()=>intro?.remove(),650)}
if(reduced){finishIntro()}else{setTimeout(finishIntro,2000)}
let ticking=false;function onScroll(){header?.classList.toggle('sticky',scrollY>90);const max=document.documentElement.scrollHeight-innerHeight;if(progress)progress.style.transform=`scaleX(${max?scrollY/max:0})`;ticking=false}addEventListener('scroll',()=>{if(!ticking){requestAnimationFrame(onScroll);ticking=true}},{passive:true});onScroll();
menuBtn?.addEventListener('click',()=>{mobileMenu?.classList.toggle('open');document.body.style.overflow=mobileMenu?.classList.contains('open')?'hidden':''});mobileMenu?.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{mobileMenu.classList.remove('open');document.body.style.overflow=''}));
function openFilm(){filmModal?.classList.add('open');document.body.style.overflow='hidden';modalVideo?.play().catch(()=>{})}function shutFilm(){filmModal?.classList.remove('open');document.body.style.overflow='';modalVideo?.pause()}watchFilm?.addEventListener('click',openFilm);closeFilm?.addEventListener('click',shutFilm);filmModal?.addEventListener('click',e=>{if(e.target===filmModal)shutFilm()});addEventListener('keydown',e=>{if(e.key==='Escape')shutFilm()});
const sections=[...document.querySelectorAll('main section[id]')],navLinks=[...document.querySelectorAll('.site-header nav a')];const navObserver=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){navLinks.forEach(a=>a.classList.toggle('active',a.getAttribute('href')===`#${e.target.id}`))}}),{rootMargin:'-40% 0px -50% 0px'});sections.forEach(s=>navObserver.observe(s));
const form=document.querySelector('#enquiryForm');form?.addEventListener('submit',e=>{e.preventDefault();const d=new FormData(form),subject=encodeURIComponent(`Aravali Reserve Enquiry — ${d.get('name')||''}`),body=encodeURIComponent(`Name: ${d.get('name')||''}\nEmail: ${d.get('email')||''}\nOrganisation / Event: ${d.get('organisation')||''}\n\nRequirement:\n${d.get('message')||''}`);location.href=`mailto:indiatryme@gmail.com?subject=${subject}&body=${body}`});
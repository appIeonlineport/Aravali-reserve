const nav=document.querySelector('header'),progress=document.querySelector('#progress'),loader=document.querySelector('#loader'),menuBtn=document.querySelector('#menuBtn'),mobileMenu=document.querySelector('#mobileMenu'),stage=document.querySelector('#bottle360'),stageImg=stage?.querySelector('img');
const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches;const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.classList.add('visible')}),{threshold:.08});document.querySelectorAll('.reveal').forEach(el=>io.observe(el));window.addEventListener('load',()=>setTimeout(()=>loader?.classList.add('hide'),500));
let ticking=false;function scrollFx(){nav?.classList.toggle('scrolled',scrollY>28);const m=document.documentElement.scrollHeight-innerHeight;if(progress)progress.style.transform=`scaleX(${m?scrollY/m:0})`;ticking=false}window.addEventListener('scroll',()=>{if(!ticking){requestAnimationFrame(scrollFx);ticking=true}},{passive:true});scrollFx();
menuBtn?.addEventListener('click',()=>{mobileMenu?.classList.toggle('open');document.body.style.overflow=mobileMenu?.classList.contains('open')?'hidden':''});mobileMenu?.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{mobileMenu.classList.remove('open');document.body.style.overflow=''}));
if(stage&&stageImg&&!reduced){stage.addEventListener('pointermove',e=>{const r=stage.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;stageImg.style.transform=`perspective(1100px) rotateY(${x*12}deg) rotateX(${-y*6}deg) translateY(${y*7}px) scale(1.025)`});stage.addEventListener('pointerleave',()=>stageImg.style.transform='')}
const form=document.querySelector('#enquiryForm'),note=document.querySelector('#formNote');form?.addEventListener('submit',e=>{e.preventDefault();const d=new FormData(form),subject=encodeURIComponent(`Aravali Reserve Private Enquiry — ${d.get('name')||''}`),body=encodeURIComponent(`Name: ${d.get('name')||''}\nEmail: ${d.get('email')||''}\nOrganisation / Event: ${d.get('organisation')||''}\n\nRequirement:\n${d.get('message')||''}`);if(note){note.textContent='Opening your email to send this enquiry to Aravali Reserve…';note.style.color='#d4ad63'}location.href=`mailto:indiatryme@gmail.com?subject=${subject}&body=${body}`});